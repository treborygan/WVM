# Synthetic fixtures only

Everything committed below this directory must be generated, non-operational test data. Do not copy real warehouse records, private migration inputs, production database content, internal paths, credentials, or proprietary assets into these fixtures.

Future fixtures should document how they were generated and use invented names, identifiers, content, and assets.
